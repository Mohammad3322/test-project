name: first-workflow

on:
  push:
    branches:
      - master

jobs:
  example-jop:
    runs-on: ubuntu-latest

    steps:
      - name: Print a welcome message
        run: echo "Welcome to our first GitHub Action Workflow!"

      - name: Checkout repitory
        uses: actions/checkout@v6.0.3

      - name: List repository files
        run: ls -la
